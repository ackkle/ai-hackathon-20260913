# Issue #7 逆算ツリー Implementation Plan

> Execute inline using superpowers:executing-plans. User authorized starting before #6 on 2026-09-13; integration with #6 remains a separate check.

**Goal:** S-08 displays the five levels of an AI tree and saves selectable, unscheduled actions for S-09.
**Architecture:** A thin page imports a feature-local client screen. Pure logic validates the existing API envelope and produces a complete ReserveState. Existing shared storage persists it. Feature-local CSS avoids frozen global files.
**Tech Stack:** Existing Next.js, TypeScript, Zod, Vitest; no dependencies added.
**Spec:** docs/specs/mvp-spec.md §6.4, §9, §14; GitHub Issue #7.

## Constraints

- Change only app/tree/page.tsx, features/tree, tests/tree.test.ts and this plan.
- Do not edit shared types/storage, global CSS/layout, feature config, other members' files.
- Sample answers require an explicit button; never replace existing plans or records.
- Mock results remain identified after reload through existing isDemo/demoPersona fields.
- No calendar success, scores, editing, recalculation, or statistics fabrication.

## Steps

- [x] Test current baseline, then add failing tests for `createTreeState(state, envelope, now, id)`: complete plan/action references, unscheduled status, no input mutation, preservation of existing data, mock labeling, invalid API rejection and missing first-day normalization.
- [x] Implement feature-local `logic.ts`. Parse `{task:'tree', isMock:boolean, data:TreeResponseSchema}`; require wish and answers; reject existing tree/plans/actions/records. Generate one plan and up to three new unique action IDs; exactly one first-day marker. Keep dates/registration null. Validate final state with ReserveStateSchema.
- [x] Add `sample.ts` using createEmptyState with a woodwork wish and sample answers, explicitly marked demo; preserve existing blank settings when starting from an otherwise empty state.
- [x] Build `tree-screen.tsx` and `tree.module.css`. Read storage after hydration, show errors, generate via POST /api/ai/tree on explicit action, reject changed storage before save, reuse saved tree on reload. Render future → goals → sourced numbers → monthly → weekly; select via /schedule?actionId=<encoded id>.
- [x] Run unit tests, typecheck, lint and production build. Browser-check 360px: sample flow, five levels, persisted reload, schedule URL, error/retry, no saved data overwrite. #6 live integration remains pending.
- [ ] Review diff, rebase latest main, commit only scoped files, publish PR with validation and pending #6 integration. Another member reviews and merges.

## Verification (2026-09-13)

- `npm test`: 56 tests passed after rebasing onto main (143c95b), including five new tree persistence tests. Initial run failed because the tree logic module did not yet exist.
- `npm run typecheck`: passed. `npm run lint`: zero errors, two existing unused-argument warnings in server/ai/index.ts.
- `npm run build`: passed.
- Browser at 360×800: all five levels, three unscheduled actions, exactly one first-day marker; document width 345px, no horizontal overflow.
- Actual POST /api/ai/tree generated and saved the sample. Production-mode link navigation reached /schedule?actionId=<saved action id>.
- Production-mode failure injection (HTTP 502): error/retry displayed, storage remained empty, restoring fetch and retrying succeeded.
- Reload restored the exact stored state, without another generation button or duplicate plan. Corrupt storage remained byte-for-byte untouched and disabled generation.
- Independent code review: no actionable W1 defects. Real-AI validation of empty levels remains a W2 concern.
- Pending: live integration from #6; date-entry behavior is #8; public deployment after review/merge.
- Board update could not be performed: GitHub API could not resolve kamekamek ProjectV2 #9.

- Latest main includes #8: browser opened the selected tree action in the real schedule form; the woodwork title and 30-minute duration matched. No reservation files changed by this PR.
