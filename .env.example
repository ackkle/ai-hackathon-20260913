# AIの呼び出しモード。mock … mocks/ai の固定JSONを返す / real … Claude API を呼ぶ
AI_MODE=mock

# Claude API のキー。AI_MODE=real のときだけ使う。コミットしない
# next dev は .env.local、wrangler dev と本番は .dev.vars / wrangler secret put
ANTHROPIC_API_KEY=
